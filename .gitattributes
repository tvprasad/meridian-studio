# Normalize line endings to LF in the repository.
# On checkout, files remain LF on all platforms (overrides core.autocrlf).

* text=auto eol=lf

# Explicitly binary — never touch line endings
*.png binary
*.jpg binary
*.jfif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
